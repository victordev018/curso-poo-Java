package rentCarExercise.model.services;

// a classe que implementar esta interface, deve implememtar seus métodos aqui declarados
public interface TaxService {

    // declaração do método tax(), que recebe um amount e deve retornar um valor de imposto
    public double tax(double amount);

}
